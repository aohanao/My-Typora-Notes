# 计算机与大模型八股（公共基础）

> **公共文档说明**：本文件是从三份项目文档（AI_Review_Copilot / Agentic_RAG / CAE_Agent）中剥离出来的通用基础知识，一份维护、三处引用。面试前统一过这一份即可，项目文档里不再重复。

---

# 一、大模型基础

## Prompt Engineering

### · Few-shot Prompting（少样本提示）

在 System Prompt 或 User Prompt 中，直接给出几个"标准问答示范"。比用长篇大论解释规则有效得多。

**为何重要**：当需要模型输出特定数据结构（如给调度算法准备输入参数）时，给它看两个正确的 JSON 样例，能极大降低解析失败的报错率。本质是利用模型的模式匹配能力，用示例"锚定"输出格式。

### · Chain of Thought（CoT / 思维链）

通过在 Prompt 中加入"请一步一步地思考（Let's think step by step）"等指令，强迫模型在给出最终答案前，先输出中间推理过程。

**为何有效**：大模型直接预测最终答案时，单步决策的搜索空间极大，容易出错。把推理拆解成多步后，每生成一步中间过程，都为下一步提供了更丰富的上下文线索，相当于在推理路径上逐步缩小搜索空间，大幅降低复杂决策的错误率。

**代价**：增加 Token 消耗和延迟。

### · ReAct（Reason + Act / 推理与行动）

Agent 的核心运行范式，是 CoT 的升级版。不仅让模型"思考"，还让模型"行动"。

**工作流**：思考（Thought：我需要知道当前装备状态）→ 行动（Action：调用 get_equipment_status API）→ 观察（Observation：解析 API 返回的数据）→ 再思考（Thought：基于数据，安排这台设备去 A 点）→ 最终回答。

**与 CoT 的区别**：CoT 只暴露推理过程，不与环境交互；ReAct 将推理与工具调用结合，形成了"思考→行动→观察→再思考"的闭环，使模型能获取外部信息来支撑推理。

### · Prompt Caching（提示词缓存）

对频繁使用的长 System Prompt 进行缓存，不重复计算其 Attention 矩阵（KV Cache）。极大降低 API 成本并大幅缩短首字生成时间（TTFT）。

**原理**：Transformer 推理时，KV Cache 存储了每个位置的 Key 和 Value 向量。对于固定的 System Prompt 部分，这些向量在首次计算后可以持久化缓存，后续请求直接复用，跳过重复的前缀计算。

## Message 四类型

在 LangChain（以及所有主流大模型 API）中，对话历史不是简单的字符串拼接，而是由**不同角色的 Message 对象**组成的。

- **SystemMessage（系统消息）**
  - 角色定位："上帝视角"或"人设设定"。
  - 作用：对话的最高指令。在这里告诉模型"你是谁"、"任务是什么"、"不能做什么"。例如 `"你是一个严谨的 CAE 领域仿真工程专家..."`。
- **HumanMessage（人类消息）**
  - 角色定位：用户的输入。
  - 作用：代表真实用户在当前轮次说的话。例如 `"什么是有限元分析？"`。
- **AIMessage（AI 消息）**
  - 角色定位：大模型的输出。
  - 作用：记录大模型在上几轮对话中给出的回答。LangChain 的 `RunnableWithMessageHistory` 就是把之前的 `AIMessage` 和 `HumanMessage` 存起来，每次提问时一起发给模型，让它拥有记忆。
  - 注：`StrOutputParser()` 就是把这个复杂的 `AIMessage` 对象"剥皮"，只提取里面的 `.content` 字符串文本。
- **ToolMessage / FunctionMessage（工具/函数消息）**
  - 角色定位：工具执行的返回结果。
  - 作用：在 Agent 项目里（比如使用 MCP 时），当模型决定调用"查规范"工具后，工具返回的查询结果就是以 `ToolMessage` 的形式发回给模型的。

## Transformer 架构

Transformer 由 Vaswani 等人于 2017 年在论文《Attention Is All You Need》中提出，彻底改变了 NLP 的技术格局，成为当今几乎所有主流大语言模型（GPT、Gemini、Llama 等）的基础架构。

**核心创新**：摒弃了传统 RNN/CNN 的串行处理方式，引入自注意力机制实现并行计算，同时解决了长距离依赖问题。整个架构由编码器（Encoder）和解码器（Decoder）堆叠组成，每层包含多头自注意力子层和前馈神经网络子层，并通过残差连接和层归一化稳定训练。

**面试踩分点**：面试官不会让你手撕底层代码，但需要能清晰讲出 Transformer 为何取代了 RNN——并行计算效率高、无串行梯度消失问题、长距离依赖通过注意力直接建模而非靠隐状态逐步传递。

### · 自注意力机制（Self-Attention）

自注意力机制是 Transformer 的核心。对于序列中的每个位置，它会同时计算该位置与序列中所有其他位置的相关度（注意力权重），再据此加权融合信息。

**计算过程**：输入向量分别乘以三个可学习矩阵，得到查询向量 Q、键向量 K、值向量 V。注意力权重 = softmax(Q·K^T / √d_k)，输出 = 权重 × V。其中 √d_k 是缩放因子，防止点积过大导致 softmax 梯度趋零。

> 类比理解：读到"银行"这个词时，人类会迅速扫描上下文——前面有"存钱""贷款"则判断是金融机构，前面有"河流""洪水"则判断是河岸。自注意力机制让模型做了类似的事：同时参考句子中所有词与当前词的关联程度，动态决定"从哪里获取多少信息"。

### · 位置编码（Positional Encoding）

自注意力机制本身是位置无关的——对"我爱你"和"你爱我"的处理完全相同，因为 Q、K、V 的计算不包含位置信息。因此 Transformer 在输入层添加位置编码，为每个 Token 的向量注入其位置信息。

**具体方式**：原始论文使用正弦/余弦函数生成固定位置编码：PE(pos, 2i) = sin(pos/10000^(2i/d))，PE(pos, 2i+1) = cos(pos/10000^(2i/d))。不同维度对应不同频率的正弦曲线，使模型能通过相对位置关系推算绝对位置。后来也有可学习位置编码（如 GPT 系列的 Learned PE）和旋转位置编码（RoPE）等改进方案。

### · Encoder-Decoder 架构辨析

- **Encoder（编码器）**：使用双向注意力，每个 Token 能同时看到前后所有 Token。代表模型：BERT。适合理解型任务（分类、抽取、语义匹配）。

- **Decoder（解码器）**：使用单向（掩码）自回归注意力，每个 Token 只能看到当前位置及之前的 Token。代表模型：GPT 系列。适合生成型任务（对话、续写、代码生成）。
- **当前主流趋势**：几乎所有主流大语言模型都采用 Decoder-only 架构。原因是：单向注意力天然适配自回归生成；在大规模预训练+指令微调范式下，Decoder-only 模型展现出了更强的泛化能力（经验性结论，学术界仍有争论）。

### · Token 与分词（Tokenization）

**概念**：Token 是大语言模型处理文本的最小单位。大模型本质上在做"下一个 Token 预测（Next Token Prediction）"。Tokenization 是预处理过程：原始文本 → Token 序列 → Embedding 向量 → 输入模型。

**分词粒度**：Token 的划分不是固定词语，而是由子词分词算法决定：

- **BPE（Byte Pair Encoding）**：GPT 系列使用。从字符级别开始，反复合并最高频的字符对，直至词表达到预设大小。例如 "unhappy" 可能被切分为 "un"+"happy"。
- **WordPiece**：BERT 使用。类似 BPE 但选择合并的标准是似然增益而非频率。
- **Unigram**：SentencePiece 使用，从大词表逐步裁剪低频 Token。

**工程意义**：Token 直接与成本（计费）和性能（上下文窗口限制）挂钩。估算规则：英文约 4 字符 ≈ 1 Token，中文约 1.5~2 汉字 ≈ 1 Token。处理越多 Token，消耗的显存和时间就越多。

## 微调

### · SFT（Supervised Fine-Tuning / 有监督微调）

当 Prompt Engineering 和 RAG 都无法满足特定领域的专业性要求时（如让模型完全掌握特定软件的专用脚本语言），需要用成千上万条高质量"问题-答案"对进行有监督训练，改变模型内部的神经网络权重，使其习得特定领域的输出模式。

**关键要点**：SFT 数据的质量远比数量重要。几千条精心标注的高质量指令数据，效果可能优于几十万条低质量数据。

### · LoRA（Low-Rank Adaptation / 低秩微调）

传统全量微调需要更新模型所有参数，消耗巨大算力。LoRA 是一种参数高效微调技术，核心思想是冻结原模型的大部分参数，只在旁边外挂一个非常小的"补丁"模块（降维矩阵 A 和升维矩阵 B）进行训练。原权重 W 的更新等效为 W + BA，其中 BA 的秩远小于 W，参数量仅为原模型的 0.1%~1%。

**变体**：QLoRA 进一步将基座模型量化为 4-bit 存储，配合 LoRA 训练，使普通开发者用单张消费级显卡（如 RTX 3090）也能微调自己的专属模型。

**面试踩分点**：重点提及 LoRA 及其变体 QLoRA。核心思想是冻结原模型权重，只训练旁路注入的降维矩阵，极大降低显存消耗。

### · Function Calling 的训练原理

模型并非真的能直接运行代码。在 SFT 阶段，将工具描述（JSON Schema 定义函数名、参数名、参数类型）和用户问题拼接输入，训练模型在需要调用工具时输出结构化的 JSON（包含 function name 和 parameters），随后由开发者在代码侧解析并执行。本质是训练模型学会了"何时输出工具调用格式"和"如何填充正确参数"这一特殊输出模式。

### · 大模型训练三阶段

- **第一阶段：预训练（Pre-training）**——无监督学习，目标函数是 Next Token Prediction。在海量互联网文本上训练，使模型习得语言知识和世界知识。这一阶段消耗的算力和数据量最大，是决定模型基座能力的核心。
- **第二阶段：指令微调（SFT）**——用高质量的人工编写"问题-答案"对进行有监督训练，激发模型的对话能力和指令遵循能力。数据量远小于预训练（通常几万到几十万条），但质量要求极高。
- **第三阶段：对齐（Alignment）**——通过 RLHF（基于人类反馈的强化学习）或 DPO（直接偏好优化）等方法，使模型输出符合人类价值观和偏好（有用、诚实、无害）。RLHF 需要训练奖励模型再用 PPO 优化；DPO 直接用偏好数据对进行训练，流程更简洁。

---

# 二、计算机八股

## RPC 完整过程

> **RPC** 的全称是 **Remote Procedure Call**（远程过程调用），是一种在**分布式系统**中常用的通信方式。它允许程序像调用本地方法一样，去调用位于**远程服务器**上的函数或过程，而无需关心底层的网络通信细节。
>
> **核心思想**是：客户端发起调用请求，RPC 框架将调用信息（方法名、参数等）进行**序列化**，通过网络传输到服务器端，服务器反序列化后执行对应方法，并将结果返回给客户端。

**完整流程（客户端 Client ↔ 服务端 Server）**：

1. **客户端调用本地桩函数（Stub）**
2. **Stub 序列化参数**——把对象/参数转成二进制字节（Protobuf/JSON/Thrift）
3. **网络传输（TCP 为主）**——序列化后的数据通过 TCP 发给服务端
4. **服务端接收，反序列化**——服务端 Skeleton（服务桩）把二进制还原成参数对象
5. **执行本地业务方法**
6. **返回结果序列化**——返回值转为二进制
7. **传回客户端，反序列化**——Client Stub 转成程序对象，代码拿到返回结果，调用结束

## TCP / HTTP 基础

### TCP（传输层协议）

- 面向连接、可靠、字节流协议；
- 负责：建立连接、数据分包重传、流量控制、拥塞控制；
- 底层通道，不关心传输内容是文本还是二进制；
- 无请求/响应概念，只收发数据流。

### HTTP（应用层协议）

- **跑在 TCP 之上**，基于 TCP 连接通信；
- 规定数据格式：请求行、请求头、请求体、响应状态码、响应头；
- 一问一答模式：一次请求对应一次响应；
- 常见版本：HTTP1.1（长连接）、HTTP2（多路复用）、HTTP3（基于 UDP QUIC）。

### TCP 三次握手（建立连接）

目标：双方确认**发送、接收能力正常**，同步序列号，协商初始窗口。

设客户端 C，服务端 S：

1. **第一次握手 C → S：SYN**——客户端发 SYN 报文，携带客户端随机序列号 ISN(c)。含义：我要连你，我的发送序号从 ISN(c) 开始。
2. **第二次握手 S → C：SYN + ACK**——服务端收到 SYN，回复：SYN（服务端自己序列号 `ISN(s)`）+ ACK 确认号 `ISN(c)+1`（确认收到客户端 SYN）。含义：收到你的连接请求，我也准备好接收数据。
3. **第三次握手 C → S：ACK**——客户端回复 ACK，确认号 `ISN(s)+1`。含义：收到你的确认，连接正式建立，可以传数据。

**白话版理解**：

- 客户端：喂，听得到吗？（SYN）
- 服务端：听得到，你听得到我吗？（SYN + ACK）
- 客户端：我也听得到，我们开始传数据吧！（ACK）

意义：确认双方的收发能力都正常，保证后续传输的可靠性。

### TCP 四次挥手（断开连接）

TCP 是全双工，双方可独立关闭发送通道，需要四次：

1. C→S：FIN（客户端不再发数据）
2. S→C：ACK（收到关闭请求，服务端继续发剩余数据）
3. S→C：FIN（服务端数据发完，关闭发送通道）
4. C→S：ACK（确认关闭，等待超时彻底释放）

### HTTP 握手流程（基于 TCP）

HTTP 没有独立握手，**先完成 TCP 三次握手，再发 HTTP 报文**：

1. 客户端和服务器完成 TCP 三次握手，TCP 连接打通；
2. 客户端发送 HTTP 请求报文（GET/POST+Header+Body）；
3. 服务器处理，返回 HTTP 响应（状态码、响应头、数据）；
4. HTTP1.0：响应完直接 TCP 四次挥手断开；HTTP1.1 默认长连接：连接复用，可连续多次 HTTP 请求，空闲后断开。

**HTTP 常见状态码（背诵）**：

- `200 OK`：请求成功（最常见）。
- `301/302`：重定向（比如网页跳转）。
- `400 Bad Request`：客户端参数传错了。
- `403 Forbidden`：没权限（比如没登录去请求 VIP 歌曲）。
- `404 Not Found`：接口地址写错了，或者资源不存在。
- `500 Internal Server Error`：后端代码崩了（这时候就该去 Linux 服务器用 `tail` 查日志了）。
- `502 Bad Gateway`：网关错误（比如 Nginx 转发到了一个死掉的后端节点）。

**GET 和 POST 的区别（经典八股）**：GET 用于获取数据，参数拼在 URL 后面，不安全且长度受限；POST 用于提交数据，参数在请求体（Body）里，更安全，支持大数据量。

## 代理

### 正向代理

国内服务器无法访问或很难访问国外服务（如 GitHub）时，在本地搭建一个代理服务器帮助访问——这就是正向代理。**配置在浏览器/客户端侧**，代理的是客户端。

### 反向代理

以淘宝为例：内部有大量服务器，但服务器之间 session 不共享。于是在入口搭一台过渡服务器，用户登录一次即可访问所有服务——这就是反向代理。对客户端完全无感知、不需要任何配置，请求发给反向代理服务器，由它选择目标服务器取数据后返回。反向代理服务器和目标服务器对外就是一个整体，暴露的是代理服务器地址，**隐藏了真实服务器地址**。**配置在服务器侧**，代理的是服务端。

## 80、443 端口

### 行业标准规定（底层根源）

TCP/IP 协议里，**知名端口（Well-Known Ports）0–1023** 由互联网标准化组织 IANA 统一分配，固定给通用网络服务：

- **80 端口 = HTTP 明文网页协议**（强制标准）
- **443 端口 = HTTPS 加密网页协议**（强制标准）

这是全球统一规则，所有浏览器、服务器厂商全部遵守。

### 浏览器的默认行为：不写端口就自动补全

浏览器内置规则：

1. 输入 `example.com`（不带端口、http 协议），浏览器自动在请求末尾拼接 `:80`；
2. 输入 `example.com`（不带端口、https 协议），浏览器自动拼接 `:443`。

### 对比非标准端口场景

后端服务跑在 8080、3000、8090 这种自定义端口：

- 必须手动写端口：`example.com:8080`
- 用户记起来麻烦，域名链接也不美观，生产环境不会直接暴露。

### 后端服务为什么不直接监听 80/443，非要 Nginx 中转？

1. **权限问题**：Linux 系统中，**1024 以下知名端口需要 root 最高权限**。SpringBoot、Node、Tomcat 这类业务程序直接用 root 运行有巨大安全漏洞，一旦程序被入侵，攻击者直接拿到服务器最高权限。Nginx 可以先占用 80/443（root 启动），再降权转发请求给无 root 权限的后端服务，兼顾端口与安全。
2. **一台机器只能一个程序占用同一个端口**：一台服务器 80 端口只能给一个程序使用。如果一台机器同时部署前端静态页面、管理后台、API 接口，多个服务无法同时监听 80，必须靠 Nginx 统一接收 80 请求，再按路径/域名分发到不同后端端口（8080、3000）。

## 负载均衡

负载均衡建立在现有网络结构之上，提供一种廉价有效透明的方法扩展服务器带宽、增加吞吐量、加强网络数据处理能力。简单说：请求使单台服务器压力过大无法承受，就搭建服务器集群分担压力；而怎么把请求分给性能各异的服务器，就是负载均衡策略要解决的问题。

**Nginx 三种负载均衡方式**：

- **轮询法（默认）**：每个请求按时间顺序逐一分配到不同的后端服务器，后端 down 掉能自动剔除。适合服务器配置相当、无状态且短平快的服务，也适用于图片服务器集群和纯静态页面服务器集群。
- **weight 权重模式（加权轮询）**：指定轮询几率，weight 和访问比率成正比，用于后端服务器性能不均的情况。当后端服务器性能存在差异时，通过配置权重让性能得到充分发挥。权重越高，被访问的概率越大。
- **ip_hash**：轮询/加权轮询存在一个问题——用户在某台服务器上登录后，第二次请求被重新定位到另一台服务器，登录信息丢失。ip_hash 按访问 IP 的 hash 结果分配，每个访客固定访问一个后端服务器，可以解决 session 问题。

## GIL

### GIL 底层规则（为什么 CPU 密集不能并行）

GIL 是一把**互斥锁**：任何时刻，一个 CPython 进程内，**最多只有 1 个线程持有 GIL**，只有拿到锁的线程才能执行 Python 字节码。

- 多核 CPU 也没用：同一进程多个内核无法同时跑 Python 代码；
- 多线程跑计算：线程轮流抢 GIL，单核并发执行，**无真正并行**，甚至频繁切换锁导致速度比单线程还慢。

举例：循环做数值运算、矩阵计算、加密哈希这类纯 CPU 计算，开多线程不会提速。

### 前置基础规则（CPython 专属）

1. **GIL 约束范围：单个进程内**。一个 CPython 进程全局只有一把 GIL；同一瞬间，进程内最多 1 个线程能执行 Python 字节码，只能占用 1 个 CPU 核心。不管开 10 个、100 个线程，同一时刻只有 1 个线程在跑代码，其余线程阻塞/等待抢锁。
2. **多进程不受 GIL 限制**：每新建 1 个 `multiprocessing` 进程，会复制一份独立 CPython 解释器、独立 GIL；操作系统可以把不同进程调度到**不同 CPU 核心**，实现真正并行。

### GIL 全局解释器锁（速记版）

1. 规则：单个进程同一时刻，仅 1 个线程能执行 Python 字节码；
2. 释放时机：遇到 IO 阻塞（socket/数据库/sleep）自动释放；纯 CPU 计算不释放；
3. 影响：CPU 密集——多线程无多核并行，效率极低；IO 密集——多线程/协程可并发，无性能瓶颈；
4. 突破 GIL 唯一方案：多进程（每个进程独立 GIL）。

## 并发 / 并行

- **并发（Concurrency）**：单核 CPU 快速切换多任务，宏观同时执行，微观交替运行。适用：IO 阻塞场景（接口、爬虫、数据库）。
- **并行（Parallelism）**：多核 CPU 同一时刻多任务同时运算，真正同步执行。适用：CPU 密集数值计算。
- 行业术语：高并发 = 海量 IO 请求并发处理，不代表多核并行。

## 进程 / 线程 / 协程

### 1. 进程

- 独立内存、独立 CPython 解释器、独立 GIL；
- 唯一能实现**多核并行**的方案；
- 缺点：创建开销极大，多进程内存重复占用。

### 2. 操作系统线程（threading）

- 共享进程内存，单进程内多线程共用一把 GIL；
- IO 阻塞时释放 GIL，可实现 IO 并发；
- 缺点：每个线程 8MB 栈内存，海量线程内存爆炸；内核切换开销高；CPU 密集无法多核并行。

### 3. 协程（gevent/greenlet、asyncio）

- 全部运行在**单个操作系统线程**，单进程单线程；
- 用户态切换，无内核参与，切换成本极低；
- 上万协程内存占用极小，解决**海量多线程的内存、切换浪费**；
- 局限：单进程受 GIL 限制，**无法多核并行**；CPU 密集任务全程阻塞，协程串行。

### 资源开销排序（从小到大）

协程（gevent）＜ 系统线程 ＜ 进程

### gevent 核心作用与定位

1. 核心价值：单线程承载上万 IO 并发，替代海量系统线程，节省内存、降低切换损耗；
2. 不能做到：突破 GIL、实现多核并行；无法解决多进程内存占用问题；
3. 生产标准架构（兼顾并行 + 高并发）：多进程（多核并行）+ 单进程内 gevent 协程（IO 高并发）。

## SQL 数据库

测试/校验场景经常需要去数据库里校验数据是否写入正确，或构造特定的测试数据。假设有两张表：`users`（用户表：id, name）和 `play_records`（播放记录表：user_id, song_name, play_time）。

- **连表查询（JOIN）**：用于把两张表的数据拼起来看。
  - **INNER JOIN（内连接）**：只查两张表里**都有**匹配的数据。场景：查出所有听过歌的用户的名字和他们听的歌。`SELECT users.name, play_records.song_name FROM users INNER JOIN play_records ON users.id = play_records.user_id`
  - **LEFT JOIN（左连接）**：以左边的表为主，左表数据全要，右表匹配不上的填 NULL。场景：查出**所有**用户的听歌记录，没听过歌的用户记录显示为空（用来测试新注册用户的数据兜底逻辑）。
- **分组与过滤（GROUP BY & HAVING）**：面试必考连招。
  - **GROUP BY**：按某个字段把数据分组，通常配合 `COUNT()`（计数）、`SUM()`（求和）使用。
  - **HAVING**：在分组之后再进行条件过滤（注意：`WHERE` 是分组前的过滤，`HAVING` 是分组后的过滤）。
  - 终极连招场景：找出今天听歌总数**大于 5 首**的用户 ID：

    ```sql
    SELECT user_id, COUNT(*) as total_songs
    FROM play_records
    WHERE play_time = '今日日期'
    GROUP BY user_id
    HAVING total_songs > 5;
    ```

### MySQL：向量数据库与传统数据库的关联

面试官其实在考"元数据（Metadata）管理"。

- **ID 映射**：向量数据库（如 Milvus/Chroma）通常只存 **Vector** 和一个 **Primary ID**。
- **关联查询**：在 MySQL 里存一份完整的业务表，其中一列存该文档在向量库里的 ID。
- **典型流程**：① 从向量库搜到最相关的 Top-K 个 ID；② 拿着这组 ID 去 MySQL 里做一次 `WHERE ID IN (...)` 的查询，拿回完整的结构化信息（如：作者、更新时间、工程规范编号）。
- **理由**：向量数据库做检索快，但做复杂字段过滤（如"只要 2024 年以后的、李工写的、PDF 格式的"）非常弱，必须配合 MySQL 这种关系型数据库做混合过滤。

## Linux 常用命令

不需要像运维那样精通 Linux，但在服务器上查 Bug（比如 FastAPI 服务报错了）是基本功。

| 场景     | 常用命令                  | 面试话术 / 实际用途                                          |
| -------- | ------------------------- | ------------------------------------------------------------ |
| 看日志   | `tail -f xxx.log`         | 接口报错时，用 `tail -f` 实时滚动查看后端报错日志，定位异常堆栈 |
| 搜日志   | `grep "Error" xxx.log`    | 日志太多时，用 `grep` 结合关键字（特定用户 ID 或 Error）过滤关键信息 |
| 看进程   | `ps -ef \| grep python`   | 确认服务进程是否存活、定位占用资源的进程                      |
| 看端口   | `netstat -tunlp`          | 接口不通时，检查 8000 端口（如 FastAPI 默认端口）有没有被监听，排查网络连通性 |
| 看性能   | `top`                     | 压测或长时间自动化脚本运行时，监控 CPU 和内存占用，看有没有内存泄漏 |

## Redis 相关

- **数据结构与适用场景**：
  - **String**：适合存简单 K-V，如验证码、简单的单一标记。
  - **Hash**：适合存对象（如电商购物车、用户详情）。在 Agent 系统中，当前 Thread 的 `CAEAgentState` 非常适合序列化为 JSON 后存入 Hash 或 String 作为会话快照。
  - **List**：适合简单的消息队列或时间线。
  - **Set/ZSet**：Set 用于去重（如点赞），ZSet（有序集合）适用于排行榜或按时间戳排序的任务。
- **过期与淘汰策略**：
  - **惰性删除 vs 定期删除**：过期 Key 不会立刻瞬间删除。Redis 采用"惰性"（访问时发现过期才删）+"定期"（后台线程随机抽取删除）结合的方式。
  - **淘汰策略**：内存打满时触发 `maxmemory-policy`。如果存的是 **Agent 会话状态**，建议用 `volatile-lru` 或 `allkeys-lru`，优先淘汰最近最少使用的长尾闲置会话。
- **缓存三大问题**：
  - **穿透**：请求根本不存在的数据（如恶意攻击 id=-1）。解决：布隆过滤器或缓存空对象。
  - **击穿**：某个热点 Key 突然过期，并发请求直接砸向 DB。解决：互斥锁（Mutex Lock），只让一个线程去查库重建缓存。
  - **雪崩**：大面积 Key 同时过期。解决：设置过期时间时加上随机数。
- **Kafka vs Redis Stream**：
  - Redis Stream 轻量，提供类似 Kafka 的消费组（Consumer Group）和 Pending List（处理中未确认的消息）。支持 ACK 确认，一定程度上保证不丢，但极端宕机下（未持久化 AOF）可能丢数据。
  - Kafka 基于磁盘日志，高吞吐，适合极高可靠性的工业大数据流。如果项目中只是做简单的节点状态广播，Redis Stream 足够。

---

# 三、同/异步 & 阻塞/非阻塞（Agentic_RAG 生产化追问专用）

## 同步、异步

- **同步**：调用函数/任务后，**调用者原地卡死，必须等任务完全执行完、返回结果，才能执行下一行代码**。
- **异步**：发起耗时任务后，**调用者不会原地等待，立刻向下执行其他逻辑**；任务在后台独立运行，完成后通过回调/事件/消息通知主线程取结果。

## 单线程、多线程（执行层面，CPU 调度单元）

- **单线程**：同一时刻只有一条执行流，CPU 分时切换任务。
- **多线程**：同一进程多条执行流，操作系统并行调度（多核可真正并行）。

## 阻塞、非阻塞

- **阻塞**：线程发起 IO/计算操作时，操作系统直接挂起当前线程，线程啥也干不了，直到操作完成才唤醒。
  - Streamlit 原生就是**阻塞 IO 模型**：整个服务只有一条主线程，任何耗时操作都会挂起线程，无法处理其他请求。
  - 长耗时计算（Marker PDF 解析）本质是 CPU 阻塞，和 IO 阻塞效果一致：线程被占用，无法复用。
- **非阻塞**：线程发起操作后，操作系统不会挂起线程，立刻返回；如果任务还没完成，返回"未就绪"，线程可以先去处理别的任务，隔段时间再查询状态。
  - 典型：FastAPI/Starlette 的非阻塞网络 IO，请求来了不卡死线程，线程可以快速释放接收下一个请求。

## 四种组合关系

1. **同步阻塞（Streamlit 默认）**：调用任务 + 线程卡死等待；最简单、并发最差。
2. **同步非阻塞**：调用任务后线程不卡死，但代码需要循环轮询任务状态；代码繁琐，工业很少用。
3. **异步阻塞**：几乎无实用场景。
4. **异步非阻塞（高并发生产标准方案，FastAPI、Tornado、Celery）**：发起任务不等待，底层 IO 不挂起线程；主线程无限快速处理海量用户请求，耗时计算丢后台队列。

## 并发、高并发

- **并发**：同一时间段内，多个任务**交替穿插执行**，宏观看起来同时跑，微观分时切换。
  - 同步阻塞单线程：不支持并发，同一时间只能处理 1 个用户，其他人排队。
  - 异步非阻塞：单线程即可实现并发，线程空闲时自动处理其他请求。
- **高并发**：系统能够**同时承载大量用户/请求**，QPS（每秒请求数）高，不会卡顿、超时、崩溃。
  - Streamlit 天生不支持高并发：单阻塞线程，多用户排队，长耗时任务直接击穿超时阈值。
  - 高并发生产架构（解决 PDF 解析痛点）：前端 → FastAPI（异步非阻塞接收上传，快速响应）→ Celery 异步任务队列（多 Worker 进程分布式跑 Marker PDF 解析，GPU 集群扩容）→ Redis/MySQL 存储解析结果 → 前端轮询/WebSocket 获取文本，完全不阻塞 Web 服务。

| 维度         | Streamlit 同步阻塞模式（Demo）    | 异步非阻塞 + 任务队列（生产高并发）  |
| ------------ | --------------------------------- | ------------------------------------ |
| 调用方式     | 同步，上传后页面死等解析完成      | 异步，提交任务立刻返回，不等待       |
| 线程模型     | 单线程阻塞，一个任务卡住全部用户  | 主线程非阻塞，瞬间释放处理新请求     |
| PDF 耗时任务 | 占用 Web 服务主线程，页面极易超时 | 交给后台独立 Worker，不占用 Web 线程 |
| 并发能力     | 极低，多用户排队，无法高并发      | 高并发，同时接收成千上百上传请求     |
| 适用场景     | 本地单人 Demo、内部小工具         | 线上生产、多用户同时使用、长耗时计算 |

## `__init__` 介绍

### `__init__` 的作用是什么？为什么要初始化 `self.chain = self.__get_chain()`？

在面向对象编程（OOP）中，`__init__` 是类的**构造函数**（初始化方法）。

**作用**：当在外面写 `rag = RagService()` 时，`__init__` 里面的代码会**且仅会执行一次**。它的主要作用是**提前准备好所有的"重型武器"和"固定流程"**。

**为什么要这样写 `self.chain = self.__get_chain()`？**

- **避免重复组装（性能优化）**：`__get_chain()` 函数里包含了非常复杂的 LCEL 管道组装逻辑（定义 Prompt、绑定模型、注入历史记忆等）。如果在每次提问（`rag.ask(query)`）时才去组装一遍这个链条，每次都要消耗额外的计算资源。
- **"组装线"常驻内存**：把 `self.chain` 放在 `__init__` 里，意味着**在服务启动的瞬间，整条流水线（Pipeline）就已经拼装完毕并静静等待了**。当用户发起提问时，数据直接流入这条已经建好的管道，速度极快。这就是经典的"单例/工程化"写法。
- **代码解耦**：把复杂的链条组装逻辑封装到私有方法 `__get_chain()` 中，让 `__init__` 看起来很清爽，这是一种很好的代码整洁习惯（Clean Code）。







# Docker

Docker 作为当前云原生和基础设施的基石，**完全能够承载重型项目**（包括百 G 内存数据库、大模型多卡训练/推理、重型单体企业系统、工业 CAE/HPC 仿真等）。

很多人误以为“Docker 只适合跑轻量微服务”，这是一种误解。Docker 底层并不是传统意义上的“虚拟化”，而是 **Linux 内核原生的进程级隔离（cgroups + namespaces）**，其 CPU/内存的性能损耗通常 **小于 1%~2%**，基本等同于裸金属（Bare-Metal）。

以下从 **底层能力边界、重型项目实战可行性、具体配置调优边界** 三个维度为你全面拆解。

---

## 一、 Docker 的底层本质与能力边界

### 1. 容器的本质：它只是一个“戴了紧箍咒”的宿主机进程
- **无 Hypervisor 开销**：容器不需要像 KVM/VMware 那样模拟硬件指令集或运行独立的 Guest OS 内核，所有容器**共享宿主机的同一个 Linux 内核**。
- **计算性能近乎零损耗**：CPU 指令和内存寻址直接由硬件执行，没有额外翻译层。

```
┌─────────────────────────────────────────────────────────────┐
│ 虚拟机 (VM)：硬件虚拟化 ──> Guest OS ──> 应用 (损耗 5%~15%)    │
├─────────────────────────────────────────────────────────────┤
│ 容器 (Docker)：宿主机内核 (cgroups/namespaces) ──> 应用 (损耗 < 1%)│
└─────────────────────────────────────────────────────────────┘
```

### 2. Docker 的“能力边界”（什么能做，什么做不到？）

| 维度             | Docker 可以做（能力范围内）                              | Docker 绝对做不到 / 极不推荐（能力边界）                     |
| :--------------- | :------------------------------------------------------- | :----------------------------------------------------------- |
| **操作系统内核** | 跑任何基于 Linux 用户态的软件（Ubuntu/CentOS/Alpine 等） | **不能跨内核**：Linux 容器无法直接调用 Windows 内核 API；不能在 x86 宿主机上原生跑 ARM 容器（需 QEMU 模拟，性能暴跌 10 倍） |
| **硬件与驱动**   | GPU（CUDA）、NPU、FPGA、RDMA 网卡、USB 设备直通          | 容器内**不能直接加载/卸载宿主机内核模块**（如 `insmod`，除非完全放开 `--privileged` 且极度危险） |
| **实时性要求**   | 毫秒级、常规企业级高并发系统                             | **硬实时系统（Hard Real-Time）**：微秒级工控系统（PLC、车载底盘控制），Linux 进程调度无法做到确定性时钟中断 |
| **存储吞吐**     | 通过 Volume 挂载裸盘或 SSD，达到裸机 99% I/O 性能        | 直接在容器可写层（Overlay2）进行海量大文件高频随机读写（会有严重的写放大与性能折损） |

---

## 二、 重型项目能通过 Docker 运行吗？

**答案是：完全可以，且业界头部企业普遍在用。**

### 典型重型项目容器化案例：
1. **重型分布式/单体数据库**：
   - **PostgreSQL / MySQL / Oracle**：生产环境单实例分配 **64 核 CPU、256GB~512GB 内存** 极其常见。
   - **ClickHouse / TiDB / Elasticsearch**：处理 PB 级时序与全文检索数据，全容器化部署。
2. **AI 大模型训练与高并发推理**：
   - 多卡 **8x H100 / A100**，利用 `nvidia-container-toolkit` 直通 GPU，运行 DeepSeek / Llama 训练、vLLM 推理服务。
3. **重型文档解析与复杂计算流水线**（如你的 AI-Review-Copilot）：
   - Docling / OCR 引擎、文档版面分析、大模型编排，通过 Docker 隔离 CPU 密集型任务，单容器分配 16~32 核。
4. **工业仿真与 CAE/HPC 计算**：
   - 利用 Singularity / Docker 运行 ANSYS、OpenFOAM 流体动力学仿真，绑定 NUMA 节点榨干多路 CPU 性能。

---

## 三、 重型项目的具体配置边界与调优清单

要让重型项目在 Docker 中发挥出裸机级性能，**必须突破 Docker 的默认配置限制**。以下是关键配置项与参数边界：

```
                              Docker 重型项目调优全景
┌────────────────────┬────────────────────┬────────────────────┬────────────────────┐
│      CPU & 算力    │    内存与共享内存   │     存储与 I/O     │     网络与连接     │
├────────────────────┼────────────────────┼────────────────────┼────────────────────┤
│ --cpus / cpuset    │ -m / --shm-size    │ Volume / Direct I/O│ --net=host / MTU   │
│ NUMA 节点绑定      │ OOM-Killer 调优    │ 避开 Overlay2 写入 │ somaxconn / 端口栈 │
└────────────────────┴────────────────────┴────────────────────┴────────────────────┘
```

### 1. 算力与 CPU 配置边界（多核与 NUMA 绑定）
*默认情况下，Docker 容器可以在所有 CPU 核心间漂移，对于重型应用会导致 CPU L1/L2/L3 缓存失效和跨 NUMA 内存访问延迟。*

- **硬核 CPU 绑定（NUMA 调优）**：
  ```bash
  # 分配 32 个指定物理核心，并绑定到特定的内存节点 0（防止跨 Socket 访问内存导致延迟增加 30%）
  docker run --cpuset-cpus="0-31" --cpuset-mems="0" my-heavy-app
  ```
- **CPU 权重与配额**：
  ```bash
  # 允许最大使用 64 个 CPU 核心的算力
  docker run --cpus=64.0 --cpu-shares=2048 my-heavy-app
  ```

---

### 2. 内存与共享内存（`--shm-size` 致命避坑）
*这是绝大多数重型项目（尤其是 AI、多进程 Python、PostgreSQL、PyTorch DataLoader）崩溃的第一原因！*

- **突破共享内存默认 64MB 的限制**：
  - **现象**：Docker 默认 `/dev/shm` 仅分配 **64MB**。重型数据库或 PyTorch 多进程读取数据时会直接触发 `Bus error (core dumped)`。
  - **配置**：
    ```bash
    # 重型 AI 训练 / 多进程应用，通常设置为宿主机内存的一半甚至更大（如 64GB）
    docker run --shm-size=64g -m 256g my-heavy-app
    ```
- **防 OOM 误杀与 Swap 保护**：
  ```bash
  # 分配 128G 物理内存，禁止向 Swap 写入（保障低延迟），并降低 OOM 被杀权重
  docker run -m 128g --memory-swap=128g --oom-score-adj=-500 my-database
  ```

---

### 3. 存储与磁盘 I/O 边界（避开 Overlay2 性能陷阱）
- **性能陷阱**：直接写在容器可写层（Overlay2）有写入重定向开销，随机 I/O 性能暴跌 30%~70%。
- **重型配置准则**：
  ```bash
  # 1. 必须使用 Volume 直通宿主机 NVMe SSD / 裸盘
  docker run -v /data/pgdata:/var/lib/postgresql/data:rw my-postgres
  
  # 2. 针对高并发读写大文件，使用直接 I/O 或内存映射
  docker run --device-read-bps /dev/nvme0n1:1000mb my-heavy-app
  ```

---

### 4. 网络性能与并发连接数边界
- **默认 Bridge 模式的开销**：Docker 默认的 Bridge 网桥经过 iptables NAT 转发，在万兆（10Gbps/100Gbps）网络或百万 QPS 下会消耗 10%~20% 的 CPU 用于软中断。
- **重型优化方案**：
  ```bash
  # 方案 A：直接使用 Host 网络（消除网络虚拟化损耗，完全达到裸机性能）
  docker run --net=host my-high-qps-app
  
  # 方案 B：修改内核参数，突破连接数与端口队列上限
  docker run --sysctl net.core.somaxconn=65535 \
             --sysctl net.ipv4.tcp_max_syn_backlog=65535 \
             --sysctl net.ipv4.ip_local_port_range="1024 65535" my-heavy-app
  ```

---

### 5. 操作系统与文件句柄上限（`ulimit`）
*重型项目启动数百个线程、打开数万个文件/Socket 时，极易触发 `Too many open files`。*

```bash
docker run --ulimit nofile=655350:655350 \
           --ulimit nproc=655350:655350 \
           --ulimit memlock=-1:-1 my-heavy-app
```
*(注：`memlock=-1` 对大模型推理锁定显存/内存物理映射至关重要)*

---

### 6. GPU 与硬件加速直通（AI / HPC 核心）
```bash
# 挂载所有 NVIDIA GPU，并共享主机 IPC 命名空间（实现多卡 GPU P2P 显存直通）
docker run --gpus all \
           --ipc=host \
           --ulimit memlock=-1 \
           --ulimit stack=67108864 \
           vllm/vllm-openai:latest
```

---

## 四、 选型决策：什么时候用 Docker？什么时候该用裸金属/虚拟机？

```
                         重型项目部署选型矩阵
                     
                  是否需要微秒级硬实时？ ──[是]──> 裸金属 / 专用工控机
                           │ [否]
                  是否依赖非 Linux 特殊内核/模块？ ──[是]──> 传统虚拟机 (KVM/ESXi)
                           │ [否]
                  是否追求极致性能 + 快速交付/隔离？
                           │
                           ▼
               【果断选择 Docker / 容器化】
        (配置好 NUMA、--shm-size、Volume 直通、--net=host)
```

### 总结
1. **能力边界**：Docker 的限制在于**必须共享宿主机 Linux 内核**且**不适合硬实时场景**，除此之外，算力、内存、I/O 几乎没有硬件上限。
2. **重型项目可行性**：几十核 CPU、数百 GB 内存、多卡 GPU 的重型系统完全可以在 Docker 下满血运行。
3. **关键在于配置**：跑重型项目绝不能用默认参数，必须配置 **`--shm-size`（共享内存）、`cpuset`（NUMA绑定）、`Volume`（裸盘直通）、`ulimit`（句柄扩容）与 `--net=host`（网络直通）**。

---

# 四、 高并发系统架构与设计全景（高并发学习总结）

> **业务背景**：在短时间内有海量用户请求高频访问系统，系统必须在承受巨大并发流量冲击的同时，保证**“高性能（低延迟、高吞吐）”与“高可用（故障隔离、容灾自愈）”**。  
> **高并发演进六大核心出发点**：**系统拆分 ── 缓存加速 ── MQ消息队列 ── 读写分离 ── 数据分离（分库分表） ── 服务监控（可观测性）**。

```
                                  高并发系统全景架构流转
                                  
  ┌────────────────┐
  │ 客户端/浏览器  │ ──> [本地缓存: Cookie/localStorage] (减少网络请求)
  └───────┬────────┘
          │ (HTTP/HTTPS)
          ▼
  ┌────────────────┐
  │ Nginx/负载均衡 │ ──> [系统拆分 & 水平扩容] (轮询 / 加权轮询 / 一致性哈希)
  └───────┬────────┘
          │
          ▼
  ┌────────────────┐      ┌─────────────────────────┐
  │ 微服务应用集群 │ <──> │ Redis 分布式缓存集群    │ (Cache-Aside: 击穿/穿透/雪崩治理)
  └───────┬────────┘      └─────────────────────────┘
          │
          ├──────────────> ┌─────────────────────────┐
          │ (异步削峰)     │ MQ 消息队列 (Kafka/RMQ) │ ──> 下游业务匀速消费 (解耦/削峰)
          │                └─────────────────────────┘
          ▼
  ┌─────────────────────────────────────────────────────────┐
  │                    数据库存储架构                       │
  │  ┌───────────────────────┐      ┌────────────────────┐  │
  │  │ MySQL 主库 (Master 写) │ ──> │ 从库集群 (Slave 读)│  │ (读写分离: 解决读性能/半同步复制)
  │  └───────────┬───────────┘      └────────────────────┘  │
  │              │                                          │
  │              ▼                                          │
  │  ┌───────────────────────────────────────────────────┐  │
  │  │ 分库分表 (水平/垂直拆分, 突破单机存储与写性能瓶颈) │  │ (数据分离: 解决写性能与单表上限)
  │  └───────────────────────────────────────────────────┘  │
  └─────────────────────────────────────────────────────────┘
          ▲
          │
  ┌─────────────────────────────────────────────────────────┐
  │ 全方位服务监控与可观测性 (Metrics 指标 + Log 日志 + Trace 链路追踪) │
  └─────────────────────────────────────────────────────────┘
```

---

## 4.1 维度 1：系统拆分与水平扩容

### 1. 核心思想：“拆分与扩容”
- **（1）业务拆分（垂直解耦）**：
  - 将传统的单体巨石应用按照业务域边界拆分为独立的**微服务**（如用户服务、订单服务、支付服务、商品服务、AI 处理服务）；
  - **价值**：每个微服务可由独立团队维护、独立部署、按需扩容；单模块崩溃不会拖垮全局，实现故障物理隔离。
- **（2）水平扩容（Scale-Out）**：
  - 通过增加服务器节点实例分担流量压力；
  - 结合**负载均衡器（Nginx / LVS / F5 / 云 ALB）**将流量均匀分摊到不同节点，消除单点瓶颈与单点故障。
  - **常见负载均衡算法**：
    - **轮询（Round Robin）**：请求按顺序逐一分配给后端服务器；
    - **加权轮询（Weighted Round Robin）**：根据机器硬件性能差异配置不同权重，性能强的节点分配更多流量；
    - **一致性哈希（Consistent Hashing）**：按请求特征（如用户 IP、User ID）哈希映射到哈希环，保障同一用户路由至固定节点（会话保持/本地缓存命中），且节点增减时仅影响少量请求。

---

## 4.2 维度 2：缓存加速（Cache）——内存级吞吐

### 1. 核心思想
> **把热点、高频访问的数据放入读写速度远快于磁盘数据库的内存中，请求优先读缓存，将 80%~90% 的流量拦截在数据库之前。**

### 2. 常见缓存层级与形态
- **（1）Redis 分布式缓存**：
  - 独立部署的高性能内存中间件，所有微服务实例共享同一套缓存集群，支持数据持久化（RDB/AOF）与主从/哨兵/Cluster 集群横向扩容。
  - **经典读写模式：Cache-Aside（旁路缓存模式）**：
    - **读流程**：先查缓存，命中则直接返回；未命中则查 DB，将 DB 结果写入缓存并返回；
    - **写流程**：**先更新数据库，再删除缓存**（依靠下一次读操作将新数据回填缓存，避免直接更新缓存带来的并发脏写问题）。
  - **高并发经典三灾与应对方案**：
    - **缓存穿透**（查询一个根本不存在的数据，缓存和 DB 均无数据，请求直击 DB）：
      - *解法*：**布隆过滤器（Bloom Filter）** 在入口快速拦截无效 Key；或对查询不到的数据缓存一个**空值（Null Object）并设置短 TTL**（如 30 秒）。
    - **缓存击穿**（单个超热点 Key 突然过期，海量并发请求瞬间冲垮数据库）：
      - *解法*：**互斥锁（Mutex Lock / 分布式锁）**，只允许一个线程去 DB 加载数据，其余线程重试等待；或设置**逻辑不过期**（后台异步线程定期刷新）。
    - **缓存雪崩**（大量 Key 在同一时间集中过期，或 Redis 集群宕机，全部流量倾泻到 DB）：
      - *解法*：Key 的过期时间增加**随机波动偏移量（Random TTL）**，避免集体失效；构建高可用 Redis Sentinel / Cluster 架构；多级缓存（本地缓存 Guava/Caffeine + 远程 Redis）。
  - **缓存与数据库一致性保障**：
    - 采用“**延迟双删**”（更新 DB 前删缓存，更新 DB 后 sleep 500ms 再次删缓存，防并发脏读写入旧缓存）；
    - 或采用“**Canal 监听 MySQL Binlog 异步删除缓存**”（解耦主业务，高可靠重试保障最终一致性）。
- **（2）浏览器与客户端本地缓存**：
  - 存储于用户前端浏览器，不发起网络请求或仅进行协商缓存，直接从本地内存/磁盘加载；
  - **Cookie**：容量小（~4KB），每次发起 HTTP 请求会自动携带至服务端 Header；
  - **localStorage / sessionStorage**：容量大（5MB~10MB），纯前端持久化/会话存储，**不会自动随 HTTP 请求上传**，适合缓存用户 UI 偏好、Token、静态字典数据。

---

## 4.3 维度 3：MQ 消息队列——异步解耦与削峰填谷

### 1. 核心思想
> **把非核心、耗时长的同步业务剥离成消息异步处理，实现系统间彻底解耦；同时缓存瞬时高峰流量，让下游按照自身处理能力匀速消费，提升主接口响应速度，保障主流程不受下游故障波及。**

```
 [用户请求] ──> [核心下单/评审主流程 (耗时 20ms)] ──立即返回响应──> [用户获得极速反馈]
                         │
                         ▼ (异步投递消息)
                ┌──────────────────┐
                │   MQ Broker 队列  │ (高可靠持久化缓冲)
                └────────┬─────────┘
                         │
        ┌────────────────┼────────────────┐
        ▼ (匀速消费)      ▼ (匀速消费)      ▼ (匀速消费)
  [积分发放服务]   [短信/微信通知服务]  [大数据离线分析/知识沉淀]
```

### 2. 两大核心价值
- **（1）异步解耦（Asynchronous Decoupling）**：
  - 将非核心操作（如发送短信、发放积分、生成统计报表、异步向量化）转化为 MQ 消息；
  - 主业务处理完核心事务后立即返回，接口响应时间从秒级降至毫秒级；当下游积分系统维护或崩溃时，主交易流程不受任何影响。
- **（2）削峰填谷（Traffic Shaping & Throttling）**：
  - 遇到秒杀大促、突发批量任务上传时，瞬间流量波峰极高，下游数据库和模型推理服务无法承受；
  - MQ 将瞬时流量全部接收并堆积在队列中，下游消费者按照自身的安全水位（如每秒消费 200 条）匀速拉取消费，平稳度过流量洪峰。

### 3. 常见 MQ 中间件选型对比
- **RabbitMQ**：基于 Erlang 开发，支持丰富灵活的路由模型（Direct/Topic/Fanout），**延迟低（微秒级）、可靠性极高**，适合企业级业务流转与中高并发场景；
- **RocketMQ**：阿里开源、专为电商金融级大促打造，支持**亿级消息堆积、分布式事务消息、定时/延时消息、顺序消息**；
- **Kafka**：为大数据与海量日志流而生，采用顺序写盘与零拷贝（Zero-Copy）技术，**吞吐量极高（百万级 TPS）**，适合大数据采集、用户行为分析与流计算。

### 4. 生产级三大核心挑战与应对
- **消息丢失问题**：
  - *生产者防丢*：开启 Confirm 确认机制，未收到 ACK 则重试；
  - *Broker 防丢*：开启消息持久化（落盘），集群模式下配置多副本同步刷盘（如 Raft/ISR 机制）；
  - *消费者防丢*：关闭 autoAck 自动应答，业务完全处理完毕后手动发送 `basicAck`。
- **消息重复消费（幂等性设计）**：
  - 网络抖动或重试必然导致重复消费；
  - *解法*：为每条消息分配**全局唯一 MessageID**，消费端利用 **Redis `SETNX` 或数据库唯一索引（防重表）** 保证消费幂等性。
- **消息顺序性保障**：
  - 同一订单/任务的状态变更必须按顺序消费；
  - *解法*：将相同业务 ID 的消息路由到**同一个 Partition / Queue**，并在消费端采用单线程消费或内存队列分发。

---

## 4.4 维度 4：读写分离——突破单机读性能瓶颈

### 1. 核心思想
> **互联网与企业系统中读写比例通常在 8:1 到 10:1（读远多于写）。通过部署“一主多从”数据库集群，主库负责处理全部写事务，从库负责分担全部查询请求，将数据库读吞吐能力线性放大。**

```
                      [微服务集群]
                     /           \
           (写操作 / INSERT/UPDATE)   (读操作 / SELECT)
                   /               \
                  ▼                 ▼
          ┌──────────────┐     ┌──────────────┐
          │ MySQL 主库   │ ──> │ MySQL 从库 1 │
          │ (Master 写)  │     ├──────────────┤
          └──────┬───────┘ ──> │ MySQL 从库 2 │ (读流量负载均衡)
                 │             ├──────────────┤
                 │ (binlog同步)│ MySQL 从库 N │
                 └───────────> └──────────────┘
```

### 2. MySQL 主从复制底层原理
1. **主库写操作落盘**：主库执行写 SQL（INSERT/UPDATE/DELETE）并提交事务后，将数据变更事件按顺序写入 **二进制日志（Binlog）**；
2. **从库 I/O 线程拉取**：从库的 `I/O Thread` 与主库建立长连接，请求指定位置之后的 Binlog 内容，并将其写入从库的 **中继日志（Relay Log）**；
3. **从库 SQL 线程重放**：从库的 `SQL Thread` 异步读取 Relay Log 中的事件，在从库中重新执行一遍相应的 SQL 操作，完成数据同步。

### 3. 核心痛点与工程治理：主从复制延迟
- **痛点**：由于主从同步存在网络传输与从库重放时间差，用户刚修改完数据立即刷新页面（写后即读），此时从库尚未同步完成，导致用户“查不到刚写入的数据”。
- **解决方案**：
  - **业务层强制路由主库**：对实时性要求极高或“写后立即读”的特定接口（如个人资料修改后跳转展示），强制走主库查询；非关键业务（如商品详情页浏览）走从库；
  - **半同步复制（Semi-Sync Replication）**：主库在事务提交后不立刻返回给客户端，而是等待至少一个从库成功接收并写入 Relay Log 后，才向客户端返回写入成功，大幅压缩延迟窗口。

---

## 4.5 维度 5：数据分离（分库分表）——突破单机存储与写性能上限

### 1. 核心思想
> **当单表数据量达到 500 万~1000 万行以上、B+ 树索引层级加深（导致磁盘 I/O 次数倍增），或单机 MySQL 的磁盘容量、写入 IOPS 与连接数达到物理上限时，必须按分片规则将海量数据打散拆分到多库多表中。**

### 2. 分库分表的四种经典形态

```
                      分库分表四维拆分矩阵
┌────────────────────────────────┬────────────────────────────────┐
│ 1. 垂直分库 (Vertical Sharding) │ 2. 垂直分表 (Vertical Partition)│
│ 按业务域拆库 (用户库/订单库/商品库)│ 大宽表拆分 (热点字段 vs Text大字段) │
├────────────────────────────────┼────────────────────────────────┤
│ 3. 水平分表 (Horizontal Table) │ 4. 水平分库分表 (最常用综合方案)│
│ 单表按哈希/范围打散为多张物理表 │ 既分库又分表，多机器多表全面打散│
└────────────────────────────────┴────────────────────────────────┘
```

- **（1）垂直分库**：
  - *方式*：按照微服务业务边界，将单一大库拆分为多个业务独立的库（如 `user_db`、`order_db`、`pay_db`）；
  - *价值*：不同库可部署在不同物理服务器上，独占独立的 CPU、内存与连接池资源，实现业务级压力物理隔离。
- **（2）垂直分表**：
  - *方式*：将一张包含几十个字段的“大宽表”进行列拆分，把高频访问的常用字段放在主表，把低频访问的扩展字段、大文本（如 `TEXT`、`BLOB`、富文本内容、大文档段落）抽离到独立的扩展表中，通过主键一对一关联；
  - *价值*：缩小单行数据体积，使 MySQL InnoDB 的一个 Page（16KB）能容纳更多行记录，显著提高 Buffer Pool 内存缓存命中率与扫描速度。
- **（3）水平分表**：
  - *方式*：保持表结构完全一致，按某种分片规则（如 `user_id % 4`）将单表千万级行数据打散到 `t_order_0`、`t_order_1`、`t_order_2`、`t_order_3` 等多张物理表中；
  - *价值*：控制每张物理表数据量在百万级以内，维持 B+ 树在 2~3 层的浅层状态，单次索引检索 I/O 降至最低。
- **（4）水平分库分表（大型高并发系统最常用）**：
  - *方式*：既拆分多个库实例（如 4 个 DB 实例），每个库内部再拆分多张表（如每个库 8 张表，共 32 张物理表）。利用分片键（Sharding Key，如 `user_id`）做哈希路由：`db_index = (user_id % 32) / 8`，`table_index = user_id % 8`；
  - *价值*：同时解决**单机存储容量超标**与**单机写 IOPS / 连接数瓶颈**。

### 3. 分库分表带来的技术挑战与解决方案
- **分布式唯一 ID 问题**：单机 `AUTO_INCREMENT` 自增主键失效。
  - *解法*：采用**雪花算法（Snowflake，64位趋势递增ID）**、美团 Leaf 或 Redis 递增生成全局唯一主键。
- **跨库跨表 JOIN 查询**：
  - *解法*：字段冗余设计（空间换时间）；业务应用层分次查询并在内存中组装（Data Stitching）；或将数据同步至 Elasticsearch / ClickHouse 构建宽表进行复杂检索。
- **跨分片分页与排序（Paging & Sorting）**：
  - *解法*：采用同分片键绑定（保证同一用户的订单全部路由在同一库表）；针对全局运营报表查询，采用中间件（ShardingSphere）多线程并发查询后归并排序，或异步同步到数仓分析。

---

## 4.6 维度 6：服务监控与可观测性（Observability）

### 1. 核心思想
> **高并发系统的复杂性使得“没有监控就等于在盲开飞机”。必须通过指标、日志、分布式追踪三大维度，实现对系统、中间件、业务健康度的全方位透视，在故障发生时秒级感知、分钟级定位瓶颈。**

### 2. 可观测性三大支柱（Three Pillars of Observability）

```
                         可观测性三大支柱 (Observability)
                                        │
        ┌───────────────────────────────┼───────────────────────────────┐
        ▼                               ▼                               ▼
  【1. 指标 (Metrics)】           【2. 日志 (Logs)】             【3. 链路追踪 (Trace)】
聚合统计系统水位与趋势          记录离散事件与异常细节          串联微服务完整调用拓扑
- CPU/内存/磁盘 I/O 占用        - 错误堆栈与业务上下文          - 跨服务 TraceID / SpanID
- 接口 QPS / RT (99线/999线)    - 结构化 JSON 格式              - 精准定位耗时短板与慢 SQL
- JVM GC 频率 / 连接池水位      - ELK (Elasticsearch/Logstash)  - OpenTelemetry / SkyWalking
- Prometheus + Grafana 看板     - 告警规则触发源头              - 识别网络抖动与级联依赖故障
```

- **（1）指标（Metrics）——“体温计”**：
  - 可聚合的时序数据，用于反映系统宏观健康状态；
  - 核心关注：**RED 指标（Rate 吞吐量、Errors 错误率、Duration 响应耗时）**与 **USE 指标（Utilization 使用率、Saturation 饱和度、Errors 错误）**；
  - 技术栈：**Prometheus + Grafana**，配置 P95/P99 延迟、CPU 突增、连接池耗尽等实时告警规则。
- **（2）日志（Logs）——“黑匣子”**：
  - 记录离散发生的关键业务动作与异常堆栈；
  - 核心要求：结构化日志输出（包含 timestamp、level、trace_id、user_id、error_message），接入 **ELK（Elasticsearch + Logstash + Kibana）或 Loki** 实现秒级全文检索。
- **（3）分布式链路追踪（Trace）——“导航地图”**：
  - 在微服务调用网状链路中，一次请求打上唯一的 `TraceID`，并在每个子调用生成 `SpanID`，记录每个服务的耗时与状态；
  - 技术栈：**OpenTelemetry、SkyWalking、Jaeger**；
  - 价值：当接口变慢时，一眼识别出具体是哪一个微服务内部的某一次数据库查询（慢 SQL）或第三方 API 造成了长耗时阻塞。
- **（4）高可用保护与熔断限流**：
  - 结合监控数据，集成 **Sentinel / Hystrix** 等防护组件：
    - **限流（Rate Limiting）**：超出系统安全水位的请求直接返回友好提示（如“排队中”），防止系统雪崩；
    - **熔断降级（Circuit Breaking）**：当下游非核心服务报错率过高时，快速熔断切断调用，直接返回 Mock 降级数据，保障核心主流程正常运行。









# 八股

---

## Redis（内存缓存与流式总线）

### 1. 通俗比喻
* **比喻**：Redis 就像你办公桌上的**“便利贴小黑板”**。
* 数据库（PostgreSQL）是地下室厚重的“档案铁柜”，查一次档案要下楼、开锁、翻纸质文档（走磁盘 I/O），耗时几毫秒到几十毫秒；而 Redis 就贴在桌前（纯内存读写），一抬头就能看到，耗时只有**微秒级**（快上千倍）。

### 2. 为什么用它？（不用它会怎样？）
* 用户每点一下页面，都要去数据库查用户的登录 Session；或者大模型每吐出一条评审意见，都得通知前端。如果全都直接轰炸数据库，并发稍微一高，数据库的 CPU 和磁盘读写立刻被打爆，整个系统直接瘫痪崩溃。

### 3. 在咱们项目中做了什么？
1. **存用户登录 Session**：用户登录后，登录态存进 Redis，验证极快。
2. **作为流式事件中转站（Redis Streams）**：Celery Worker 在后台调用大模型生成评审意见时，每写好一条就用 `XADD` 扔进 Redis，Django 用 `XREAD` 实时读出来，通过 SSE 实时推给前端用户。

### 4. 面试必考场景八股

#### 场景 1：缓存雪崩（Cache Avalanche）
* **面试官问**：“什么是缓存雪崩？怎么解决？”
* **通俗理解**：便利贴上的备忘录全部在**同一时间点统统过期失效了**，或者 Redis 服务器直接宕机了。此时几万个用户请求冲过来，发现 Redis 里全都没有，所有请求像雪崩一样全砸向底层的数据库，把数据库瞬间冲垮。
* **解决方案（背这三点）**：
  1. **过期时间加随机值（最常用）**：在设置缓存过期时间（TTL）时，不要设为固定的 30 分钟，而是加上一个 1~5 分钟的随机数（如 30 分钟 + 随机秒），让 Key 均匀散开过期。
  2. **做高可用架构**：搭建 Redis 主从哨兵（Sentinel）或 Redis Cluster 集群，一台挂了立即自动切换备用机。
  3. **服务熔断与降级**：当数据库压力过大时，对非核心业务开启降级，返回默认值，保护数据库。
* **面试回答模板**：“缓存雪崩是指大量缓存数据在同一时间集中过期，或 Redis 服务宕机，导致瞬间所有流量直接打到底层数据库引发崩溃。在开发中，最直接有效的做法是在基础过期时间上增加一个随机偏差，避免同时过期；同时在运维层面搭建 Redis 哨兵或集群保证高可用，并在网关层配合限流降级保护数据库。”

#### 场景 2：缓存击穿（Hotspot Invalid）
* **面试官问**：“什么是缓存击穿？和雪崩有什么区别？”
* **通俗理解**：雪崩是“成千上万个 Key 同时死”，击穿是“**某一个超级爆款热点 Key 突然过期死掉的一瞬间**”。比如某个大促爆款商品，每秒有 10 万人查，就在缓存失效的这一秒钟内，这 10 万个请求同时发现缓存没了，全冲向数据库，把数据库在这一秒内打崩。
* **解决方案**：
  1. **互斥锁（Mutex Lock）**：当第一个请求发现缓存失效时，先抢一把分布式锁，抢到锁的那个线程去查数据库并回填缓存；其他 99999 个线程抢不到锁就稍等重试，这样只有 1 个请求能到达数据库。
  2. **逻辑永不过期**：不给该热点 Key 设置 Redis 真正的过期时间，而是把过期时间写在 Value 里面，由后台异步线程去定期刷新它。

#### 场景 3：缓存穿透（Cache Penetration）
* **面试官问**：“什么是缓存穿透？怎么防御？”
* **通俗理解**：有黑客故意拿一个**数据库里压根就不存在的 ID**（比如 ID = -99999）疯狂发起请求。因为缓存里永远没有，每次都要去数据库查一遍，结果数据库查完也是空的。这种请求“直接穿透”了缓存这堵墙，永远都在消耗数据库资源。
* **解决方案**：
  1. **缓存空对象（Cache Null）**：数据库查不到，也把这个不存在的 key 存进 Redis，设置一个很短的过期时间（比如 30 秒或 1 分钟），下次同样的请求就能被拦截在 Redis 层。
  2. **布隆过滤器（Bloom Filter）**：在请求到达 Redis 之前加一层布隆过滤器。把数据库里所有存在的合法 ID 提前放入布隆过滤器，凡是它判定一定不存在的数据，在最外层直接拦截返回，根本不走后续流程。

#### 场景 4：Redis 持久化（RDB vs AOF）
* **面试官问**：“Redis 数据在内存里，服务器断电了怎么办？RDB 和 AOF 有什么区别？”
* **通俗对比**：
  * **RDB（快照）**：每隔几分钟拍一张当前内存的照片存成磁盘文件（`dump.rdb`）。
    * *优点*：文件小，系统重启恢复极快。
    * *缺点*：两次拍照之间的数据会丢失（比如每 5 分钟存一次，第 4 分钟断电，这 4 分钟数据全丢了）。
  * **AOF（命令日志）**：把执行过的每一条写命令（如 `SET name zhangsan`）都一行行追加写到文本日志里。
    * *优点*：安全，最多只丢 1 秒钟的数据（配置 `appendfsync everysec`）。
    * *缺点*：文件体积大，恢复数据时要一条条重新跑命令，启动速度慢。
* **项目实战回答**：“生产环境一般推荐**混合持久化（AOF-RDB 混合）**，既用 RDB 保证快速恢复，又用 AOF 记录增量增补，兼顾安全与速度。”

---

## RabbitMQ（消息队列 / MQ）

### 1. 通俗比喻
* **比喻**：RabbitMQ 就像小区的**“快递菜鸟驿站”**。
* 你（用户）寄快递（上传几十兆的 Word 方案），快递员（Django）如果必须亲自把快递送到千里之外的客户手上才能回来接待下一个寄件人，那大厅就会排长队甚至打架瘫痪。现在快递员直接把包裹往驿站货架（RabbitMQ 队列）上一扔，给你一张小票（任务已创建），就立即接待下一位客户。派件员（Celery Worker）在后台一件一件慢慢送。

### 2. 为什么用它？（不用它会怎样？）
* 如果不用 MQ：Docling 解析 Word 文档需要 3 分钟，大模型评审一个文档需要 10 分钟。如果在 HTTP 接口里同步硬等，浏览器 60 秒就会超时报错（504 Gateway Timeout），用户界面直接卡死报系统错误。有了 MQ 才能实现**异步解耦**与**削峰填谷**。

### 3. 在咱们项目中做了什么？
* 划分了 **3 个独立的队列**：
  1. `doc-parser` 队列：专门排队等待解析大型 Word 文档。
  2. `llm-lite` 队列：专门排队处理抽取条文等短文本任务。
  3. `llm-main` 队列：专门排队处理耗时极长的评审意见生成。
* **为什么分队列**？防止大模型评审耗时太长的“大货车”堵死通道，导致短文本抽取的“自行车”也过不去。

### 4. 面试必考场景八股

#### 场景 1：如何保证消息 100% 不丢失？（高频死穴题）
* **面试官问**：“如果系统发生故障，消息从生产者到消费者过程中丢了怎么办？”
* **标准三步回答（全链路保障）**：
  1. **生产端防丢**：开启 RabbitMQ 的 **Publisher Confirm（发布者确认机制）**。生产者把任务发出去后，必须等到 RabbitMQ 返回 `ack` 确认包；如果返回 `nack` 或超时，生产者自动重试。
  2. **MQ 自身防丢**：开启**持久化**。包括交换机持久化、队列持久化（`durable=True`），以及消息投递模式设置为持久化（`delivery_mode=2`），消息一旦进入 MQ 就立即刷盘存进磁盘。
  3. **消费端防丢**：**关闭自动 ACK，改用手动 ACK**（在 Celery 里配置 `acks_late=True`）。必须等到 Worker 把业务真正执行完毕、数据入库成功后，才向 MQ 回复 ACK；如果 Worker 中途断电崩了，MQ 发现连接断开，会把消息重新放回队列让其他 Worker 执行。

#### 场景 2：消息重复消费了怎么办？（幂等性设计）
* **面试官问**：“网络抖动导致消费者执行完了业务，但 ACK 没发回给 MQ，MQ 重新投递，导致任务跑了两次怎么办？”
* **通俗理解**：你点了一份外卖，付了钱。外卖小哥送到了，但他手机没信号没点“送达”，系统以为没送又派了另一个小哥送了一份。
* **解决方案（幂等性）**：保证**同一条消息无论被执行多少次，最终结果都一模一样**。
  1. **全局唯一业务 ID**：每条消息带一个唯一 ID（在咱们项目中是 Task 的 `UUID`）。
  2. **状态机与前置检查**：消费端执行时，先去查数据库：“这个任务是不是已经是完成状态了？”如果是，直接跳过回复 ACK；或者利用 Redis 的 `SETNX`（分布式锁）记录“正在处理中”，同一个任务只允许被处理一次。

#### 场景 3：消息大量堆积怎么办？（积压排查）
* **面试官问**：“如果上线突发洪峰，RabbitMQ 队列里堆积了几万条消息消费不完怎么办？”
* **解决方案**：
  1. **查明根因**：先看是下游消费端挂了、死锁了，还是被大模型接口卡住了。
  2. **紧急扩容 Worker**：临时调大 Celery Worker 容器数量或并发协程数（比如从 100 临时提到 300），加速消费。
  3. **架构拆分导流**：如果消费端逻辑出 Bug，先紧急上线一批仅负责消费并把消息转存到备用临时表/死信队列的临时 Worker，把主队列疏通，等修复后再离线跑批量数据。

### 5. 消息队列间的传递

> 核心答案一句话：**它不是"检测到完成"再触发，而是 `.delay()` 就写在任务函数体的成功路径末尾——Celery 任务是普通 Python 函数，worker 同步执行到那一行，本身就意味着前面所有步骤已经完成且没抛异常。**
>
> 再拆开讲几个关键点：
>
> **1. "知道完成" = 执行流本身，不需要外部感知**
>
> Celery 任务没有"完成回调"机制，`parse_task_document` 就是一个被 worker 同步调用的 Python 函数。触发下一跳的代码和业务代码在同一个函数里、同一调用栈上——代码能走到第 364 行，就是"完成"的定义。
>
> **2. 触发时机由代码位置固定，三条流水线都是同一模式**
>
> | 任务                       | 触发点位置   | 触发前已完成的事                       | 保证"完成"的机制    |
> | -------------------------- | ------------ | -------------------------------------- | ------------------- |
> | `parse_task_document`      | tasks.py:364 | 解析、`doc.content` 落库、置 REVIEWING | 顺序执行 + 异常短路 |
> | `parse_guideline_document` | tasks.py:96  | 解析、`guideline.content` 落库         | 同上                |
> | `settle_rule_from_comment` | tasks.py:754 | 规则落库、关联 comment                 | 同上                |
>
> **3. 两个容易被忽略的细节**
>
> - **首跳特意放在事务外面**：`views/tasks.py:222` 注释写着 *"Fire async document parsing → comment generation (outside transaction)"*。如果 `.delay()` 放在 `with transaction.atomic()` 块内，消息可能比事务提交先到达 worker，worker 查 Task 会 `DoesNotExist` 直接 abort。放外面保证 worker 一定查得到。
> - **`.delay()` 是异步的**：它只负责"发消息"，发布完立刻返回，不等下游跑完。所以第 ③ 步不会因为下游慢而阻塞本 worker。
>
> **4. 一个真实存在的可靠性缺口（值得知道）**
>
> 手写接力（fire-and-forget）没有 Celery chain/link 兜底。如果 worker 在"落库之后、`.delay()` 之前"被硬杀（断电/SIGKILL），消息会因 `acks_late` 被重新投递重跑，但重跑时幂等检查发现 status 已是 REVIEWING 会直接 return——**接力就丢了，任务会卡死在 REVIEWING 状态**。超时（SoftTimeLimitExceeded）路径不会丢（置 FAILED 后重跑仍允许），但进程级崩溃有这个窗口。这也是为什么生产环境通常用 Celery `chain` + `link_error`，或让 worker 崩溃后由 Beat 巡检补跑。

---

## PostgreSQL（关系型数据库）

### 1. 通俗比喻
* **比喻**：PostgreSQL 是系统的**“官方永久铁账本”**。
* 任何任务的状态、用户的评审意见、关联的国标规范、专家知识，都必须白纸黑字严格存进磁盘。即便断电，铁账本上的数据也绝对不能丢、不能错。

### 2. 为什么用 PostgreSQL 而不是 MySQL？（经典选型考点）
* **面试官问**：“你们项目为什么选用 PostgreSQL 而不是大家常用的 MySQL？”
* **必背答案（三个核心杀手锏）**：
  1. **半结构化与 JSONB 优势**：大模型和文档解析出来的内容，很多包含不固定的属性或结构化大文本。PostgreSQL 原生支持高性能的 **JSONB 数据类型**，并且可以针对 JSON 内部字段建立 **GIN（倒排索引）**，检索速度极快；而 MySQL 对复杂 JSON 内部建索引支持较弱。
  2. **并发读写能力（MVCC 机制）**：PostgreSQL 采用堆表结构，更新操作不会像 MySQL 那样极度依赖集中式的 Undo Log 回滚段，在处理长文本读写、复杂事务时锁粒度更灵活，并发性能更稳定。
  3. **原生拓展能力与规范严谨度**：PostgreSQL 对复杂 SQL 语法、数组、全文检索、UUID 原生类型的支持比 MySQL 更加规范且丰富。

### 3. 在咱们项目中做了什么？
* 存储任务（Task）、评审意见（Comment）、4 类引用关系、国标、知识库。
* **悲观锁应用**：在合并批注范本（Sample）时，使用 `select_for_update()` 对相邻重叠文本的行加锁，防止多个人同时点合并导致文字拼接错乱。

### 4. 面试必考场景八股

#### 场景 1：索引失效（为什么明明建了索引，SQL 还是慢？）
* **面试官问**：“你在开发中遇到过哪些索引失效的情况？”
* **必背 5 大经典场景（能答出 3 个就及格，全答出是满分）**：
  1. **使用了函数或计算**：比如 `WHERE SUBSTRING(name, 1, 3) = 'abc'` 或 `WHERE id + 1 = 10`，索引只能针对原始值，套了函数数据库无法直接走 B-Tree。
  2. **隐式类型转换**：比如字段是字符串 `varchar`，查询时写成了数字 `WHERE phone = 13800000000`，数据库底层会隐式转换类型导致全表扫描。
  3. **模糊查询以通配符开头**：`WHERE name LIKE '%建筑'`，因为 B-Tree 是从左往右排序的，前缀不确定就无法用二分查找定位。
  4. **违背最左前缀法则**：建了联合索引 `(a, b, c)`，查询条件却只写了 `WHERE b = 1 AND c = 2`（缺了左边的 `a`）。
  5. **OR 条件有一侧没建索引**：`WHERE a = 1 OR b = 2`，如果 `b` 没索引，数据库只能全表扫描。

#### 场景 2：事务四大特性（ACID）与隔离级别
* **面试官问**：“事务隔离级别有哪些？分别解决什么问题？”
* **通俗速记**：
  * **未提交读（Read Uncommitted）**：能看到别人没提交的数据。导致**脏读**（别人回滚了，你看了空气）。
  * **已提交读（Read Committed，PostgreSQL 默认）**：只能看别人已提交的数据。避免脏读，但可能**不可重复读**（你前后读了两次，中间别人提交了修改，两次结果不一样）。
  * **可重复读（Repeatable Read）**：同一个事务内，不管别人怎么改并提交，你看的数据始终和事务刚开始时一样。可能发生**幻读**（你查总数是 10 条，别人新插入了 1 条提交，你再次查多出一条像幽灵一样）。
  * **串行化（Serializable）**：彻底排队，性能最低，最安全。

---

## Celery Worker 与 gevent（后台工作协程）

> **Celery 是 Python 的分布式任务队列框架**
>
> ![image-20260904110715670](C:\Users\MAH\AppData\Roaming\Typora\typora-user-images\image-20260904110715670.png)

### 1. 通俗比喻

* **比喻**：Celery 是**“工程施工队的包工头”**，负责把任务分发给工人。
* **gevent 是“会分身术的工人”（协程）**。
  * 普通多进程（prefork 模式）：1 个工人只能盯着 1 个炉子。如果要看 100 个炉子，得雇 100 个工人，公司宿舍（系统内存）瞬间被撑爆。
  * gevent 协程模式：只有 1 个工人，但他手脚极快。他去大模型接口下了一个单（发起网络请求），在大模型思考的这几秒钟里，这个工人不坐着傻等，而是转头去给第二、第三个任务发请求，谁有回应他就去处理谁。

### 2. 为什么用 gevent？
* 大模型调用和文档下载大部分时间都在**等网络返回（属于纯粹的 I/O 密集型任务）**。如果用 Python 默认的多进程 Worker，跑 100 个任务需要开 100 个进程，内存直接爆到十几 G。而用 `gevent -c 100`，单进程内存只需几十兆，就能同时挂起处理 100 个大模型连接。

### 3. 面试必考场景八股

#### 场景 1：进程、线程、协程有什么区别？
* **面试官问**：“请说一下进程、线程和协程的区别与适用场景。”
* **标准回答**：
  * **进程（Process）**：操作系统资源分配的最小单位，拥有完全独立的内存空间。切换开销最大。适用场景：CPU 密集型计算（如复杂的科学计算、图像渲染）。
  * **线程（Thread）**：CPU 调度的最小单位，同进程内多个线程共享内存空间。在 Python 中受全局解释器锁（GIL）限制，同一时刻只能有一个线程执行 Python 字节码。
  
  > ❌误区：Python 多线程不能并发。 ✅真相：**CPU 计算不能并发；IO 等待可以并发。**
  
  * **协程（Coroutine / 如 gevent）**：用户态的轻量级线程，无需内核介入，在代码级别通过事件循环主动让出执行权。切换开销极小（只有纳秒级），不需要操作系统上下文切换。适用场景：高并发网络 I/O 密集型操作（如咱们项目中的大模型 API 交互、实时推流）。
  
  > 协程的前提：**必须全部代码都是 async 兼容的 IO，不能有阻塞的同步调用 (time.sleep、requests 同步库)**。一旦出现阻塞同步调用，整个线程全部卡死，所有协程全部挂住。

#### 场景 2：任务运行时间太长卡死怎么办？（Time Limit 机制）
* **面试官问**：“后台任务如果有 Bug 陷入死循环，一直占着资源不释放怎么解决？”
* **标准回答**：“在 Celery 中设置双重超时机制：
  1. `soft_time_limit`（软超时）：超时时抛出 `SoftTimeLimitExceeded` 异常。我们可以在任务代码里捕获该异常，做清理善后工作（比如把任务状态改成 `FAILED`，给用户界面发送失败提示）。
  2. `time_limit`（硬超时）：如果软超时后代码依然没退出，硬超时到达时系统直接发送 `SIGKILL` 信号强制杀掉工作协程/进程，防止系统资源被永久霸占。”

---

## Nginx（反向代理与网关）

### 1. 通俗比喻
* **比喻**：Nginx 是整栋写字楼的**“前台总机与安保大爷”**。
* 外面的访客（浏览器用户）绝不允许随意跑到内部办公室（Django 容器或底层数据库）。所有进出请求必须先经过 Nginx 前台，由前台负责指路、检查身份证（安全防护）、拦截可疑人员（防爆破限流）。

### 2. 为什么用它？（不用它会怎样？）
* 如果直接把 Django 暴露在公网上：
  1. Django 本身处理静态资源（HTML、JS、CSS）非常慢且耗资源；
  2. 遇到黑客恶意疯狂高频撞库刷登录，Django 很快就会被请求拖垮；
  3. 无法平滑处理大文件断点续传和 SSL 证书卸载。

### 3. 面试必考场景八股

#### 场景 1：正向代理 vs 反向代理
* **面试官问**：“什么是正向代理？什么是反向代理？”
* **通俗解释**：
  * **正向代理（买方找代购）**：代理的是**客户端**。比如你为了访问外网搭建的 VPN，目标服务器只知道代购的 IP，不知道真正的你在哪里。
  * **反向代理（找公司总机）**：代理的是**服务端**。比如你访问 `www.taobao.com`，你以为你访问的是一台电脑，其实你只碰到了 Nginx，Nginx 背后把请求悄悄转发给成千上万台服务器集群。

#### 场景 2：系统被黑客写脚本恶意暴力刷登录接口怎么办？（Nginx 限流）
* **面试官问**：“你们项目是怎么防止接口被刷爆的？”
* **标准回答**：“我们在 Nginx 层使用了基于**漏桶算法（Leaky Bucket）**的限流模块 `limit_req`：
  ```nginx
  limit_req_zone $binary_remote_addr zone=login:10m rate=1r/s;
  location = /api/auth/login/ {
      limit_req zone=login burst=5 nodelay;
      proxy_pass http://django:8000;
  }
  ```
  限制同一个客户端 IP 每秒最多只能发起 1 次登录请求，允许最多突发 5 次。超出的恶意请求在 Nginx 层直接返回 HTTP 429 Too Many Requests 拒绝，压根不会打到后端的 Django 业务系统。”

#### 场景 3：为什么大模型流式输出（SSE）在 Nginx 后面会变成卡顿、一次性吐出一大坨？
* **面试官问**：“大模型流式响应经过 Nginx 转发时，前端没有一个个字吐出来的打字机效果，而是等了 10 秒钟突然弹出一大段，怎么排查？”
* **标准回答**：“这是因为 Nginx 默认开启了**反向代理缓冲区（`proxy_buffering on`）**。Nginx 以为这是一个普通的网页文件，会尽量攒够一个数据包（比如 4KB 或 8KB）才集中发送给客户端。
  * **解决方案**：在 Nginx 对应的流式路径下显式关闭缓冲，配置 `proxy_buffering off;` 和 `proxy_cache off;`，同时调大 `proxy_read_timeout` 超时时间，保证后端大模型吐出一个字，Nginx 就能立刻给前端推一个字。”

---

## Django & Web 架构

### 1. 通俗比喻
* **比喻**：Django 是整个系统的**“业务大管家”**。
* 接收前台的指令，查验权限合不合法，组织数据格式，指派任务，是连接数据库、MQ 和用户界面最核心的纽带。

### 2. 面试必考场景八股

#### 场景 1：ORM 的 N+1 查询问题（高频考点）
* **面试官问**：“什么是 ORM 的 N+1 问题？怎么解决？”
* **通俗理解**：查询 100 篇评审意见（查了 1 次 SQL），每篇意见都有作者和关联的方案。写代码时如果用 `for comment in comments: print(comment.task.name)`，代码在循环里针对每个 comment 又去执行了 1 次 SQL 查关联任务，最终发了 1 + 100 = 101 次 SQL 查询，直接把数据库拉爆。
* **解决方案**：
  * 用 `select_related()`：底层翻译成 SQL 的 **`INNER JOIN` 联表查询**，一次把两张表合在一起查回来（适合一对一、一对多外键）。
  * 用 `prefetch_related()`：底层使用 **`IN (id1, id2, ...)` 查两次**，在 Python 内存中自动拼装（适合多对多关联）。

#### 场景 2：CSRF 攻击（跨站请求伪造）
* **面试官问**：“什么是 CSRF 攻击？Django 怎么防御的？”
* **通俗理解**：你刚刚登录了网银，浏览器存了 Cookie。此时你点开了一个恶意钓鱼网页，该网页悄悄向网银发起一个转账请求。由于浏览器会自动带上你原本合法的 Cookie，网银以为是你本人操作，转账成功。
* **解决方案**：Django 内置了 `CsrfViewMiddleware`。每个表单和敏感请求必须携带服务端生成的一个不可伪造的随机秘钥 `csrf_token`。钓鱼网站无法窃取到该 Token，请求就会被拒绝。

---

## SeaweedFS（分布式对象存储）

### 1. 通俗比喻
* **比喻**：SeaweedFS 是系统的**“大件集装箱仓库”**。

### 2. 为什么不用 PostgreSQL 数据库直接存文件？
* **面试官问**：“为什么用户上传的几十兆 Word 文档不直接作为二进制字段存进数据库？”
* **标准回答**：
  1. 关系型数据库的单行数据如果太大，会导致数据库页拆分、内存脏页置换频繁，数据库缓存池（Buffer Pool）命中率暴跌；
  2. 数据库做主从同步、全量备份容灾时，巨大体积的文件会导致网络 I/O 堵塞；
  3. 对象存储专为大文件、海量文件寻道优化，便宜、高并发读写能力强，并且支持断点续传与 CDN 加速。

---

## 核心逻辑速查记忆卡（面试前 5 分钟背诵）

| 组件              | 它在项目中是什么角色？            | 面试核心考点 / 常见事故      | 解决核心关键词                                    |
| :---------------- | :-------------------------------- | :--------------------------- | :------------------------------------------------ |
| **Redis**         | 存 Session、中转大模型流式事件    | 缓存雪崩、击穿、穿透         | 随机过期时间、互斥锁、布隆过滤器                  |
| **RabbitMQ**      | 异步削峰、任务缓冲（分 3 个队列） | 消息丢失、重复消费、积压     | 手动 ACK、消息持久化、唯一业务 ID 幂等            |
| **PostgreSQL**    | 存核心数据、多租户隔离、悲观锁    | 索引失效、事务隔离级别       | 最左前缀、`select_for_update` 悲观锁、JSONB 倒排  |
| **Celery+gevent** | 异步包工头 + 100 协程并发处理     | 协程 vs 线程、任务超时卡死   | 协程专治 I/O 密集型、软硬超时双控                 |
| **Nginx**         | 外部反代、静态托管、网关安全防护  | 暴力刷接口、SSE 流式卡顿     | 漏桶限流、`proxy_buffering off` 禁缓冲            |
| **Django**        | HTTP 门面、权限校验、业务逻辑处理 | N+1 查询问题、CSRF 安全      | `select_related` / `prefetch_related`、CSRF Token |
| **SeaweedFS**     | S3 兼容对象存储、存 Word 大文件   | 为什么不用关系型数据库存文件 | 数据库 Buffer Pool 污染、备份网络阻塞             |

## 具体做法

在本项目（施工方案智能评审系统）中，各技术栈协同构建了一条完整的**高可靠流式大模型评审流水线**：

```
[浏览器 Vue 3 SPA] 
       │ HTTP / SSE
       ▼
[Nginx :80] ──静态资源直接返回 / /media/ 代理
       │ 
       ├─► [SeaweedFS :8333] (S3 对象存储，存 Word 文档)
       │
       ▼
[Gunicorn (gevent) / Django 5.2 :8000]
       │
       ├─► [PostgreSQL 18] (业务模型、多租户权限、悲观锁合并)
       │
       ├─► [RabbitMQ 4.2] (Celery 3条独立队列)
       │        │
       │        ▼
       │   [Celery Worker x3 (gevent -c 100)] 
       │        │ (1. parse_task_document -> Docling)
       │        │ (2. extract_clauses / generate_knowledge_summary)
       │        │ (3. generate_comments -> LLM Agent 流式生成)
       │        │
       │        ▼
       └─► [Redis 7] (Redis Streams: XADD 发布事件 / XREAD 消费推回)
                │
                └─► Django StreamingHttpResponse ──SSE──► 前端实时渲染
```

### 1. PostgreSQL 18（核心数据骨干）
* **存储实体**：
  * 评审任务模型 `Task`、施工文档 `TaskDocument`。
  * 评审意见 `Comment` 及其 4 种关联关系：`CommentDocumentRef`（方案原文定位快照）、`CommentGuidelineRef`（指南条文）、`CommentStandardRef`（国标规范）、`CommentKnowledgeRef`（专家知识）。
  * 支撑库：`Guideline` + `GuidelineClause`、`Standard`、`Knowledge`、`Sample`（范本）。
* **关键设计与用法**：
  * **主键规范**：全业务模型统一采用 `uuid.uuid4`，避免自增 ID 遍历风险并适配分布式场景。
  * **悲观锁并发合并**：在批注范本沉淀（Sample）时，采用 `Sample.objects.select_for_update()` 针对相邻或重叠的样本片段加行排他锁，防止并发操作导致文本合并错乱。
  * **多维度访问控制**：通过 `is_public`、`allowed_users`、`allowed_groups` 结合 Django ORM Q 查询在数据库层直接实现多租户与权限隔离。

### 2. SeaweedFS（S3 对象存储）
* **具体做法**：
  * 在 Docker 中以 `seaweedfs:8333` 运行，提供兼容 S3 的存储桶（`arc`）。
  * Django 采用 `django-storages` + `boto3` 将文件存储无缝接入。
* **核心业务逻辑**：
  * **内容寻址与哈希去重**：上传的施工方案计算 MD5，保存路径严格命名为 `doc/{md5}/{filename}`。如果已有相同 MD5 的文件则直接复用，避免海量大型 docx 重复占用存储。
  * **TOCTOU（检查-使用时间差）保护**：在上传过程中利用事务与临时标志避免并发上传覆盖，若数据库入库失败则自动触发孤儿文件清理。

### 3. Redis 7（缓存、Session 与流式事件总线）
* **具体做法**：
  * **DB0** 用于标准 Django Cache 与用户 Session。
  * **Redis Streams 作为事件流总线**：每个任务创建对应的流 Key：`{entity_type}:{entity_id}:events`。
  * **Celery 写入端**：Worker 处理任务或 Agent 产生评审意见时，通过 `_publish_event()` 调用 `pipe.xadd()` 将事件（如 `status_update`、`new_comment`、`replace_comments`）推入流中，并自动设置 24 小时过期清理。
  * **全局单例连接池**：在 [redis.py](file:///d:/GitHub/ai-review-copilot/backend/api/utils/redis.py) 中利用 `@lru_cache(maxsize=1)` 维护单例连接池，严禁任何地方 `client.close()`，适配高并发下的连接复用。

### 4. RabbitMQ 4.2（Celery 异步调度中枢）
* **具体做法**：
  * 承担 Celery 的 Broker，设置严格的持久化与 ACK 机制。
  * **三队列物理隔离**：根据任务特性与耗时拆分为 3 个独立队列：
    1. `doc-parser`：用于 Docling 文档解析（超时：`soft=60min`, `hard=62min`）。
    2. `llm-lite`：用于指南条文抽取、知识库摘要（短周期 LLM 任务，`soft=30min`）。
    3. `llm-main`：用于耗时极长的大模型方案评审流式生成（超时：`soft=18h`, `hard=20h`）。
  * **避免阻塞**：防止长耗时的评审任务挤占轻量级的抽取任务。

### 5. Celery Worker（流水线异步工作集群）
* **具体做法**：
  * **启动参数**：统一采用 `-P gevent -c 100 -l INFO`，开启 100 协程并发。
  * **关键配置**：`acks_late=True`，`prefetch_multiplier=1`。
* **执行任务流**：
  1. `parse_task_document`：后台调用 Docling 将大型 docx 解析为规范化的 Markdown 文本，存入 `TaskDocument.content`。
  2. `generate_comments`：装配上下文，调用 LangGraph / LLM Agent，通过 Generator 模式逐条输出意见，入库后触发 SSE 推送。

### 6. Django 5.2 + DRF（业务与流式 HTTP 门面）
* **具体做法**：
  * 提供 RESTful API 进行鉴权、权限过滤、方案元数据管理。
  * **SSE 实时流**：在 [sse.py](file:///d:/GitHub/ai-review-copilot/backend/api/views/sse.py) 中利用 `StreamingHttpResponse` 搭配自定义的生成器 `_read_stream()`，内部持续以 `client.xread(block=15000)` 阻塞读取 Redis Streams，并注入 `: heartbeat\n\n` 保活。

### 7. gevent（高并发 I/O 支撑）
* **具体做法**：
  * **Gunicorn Web 端**：使用 `--worker-class gevent --workers 16 --worker-connections 100`。因为 SSE 连接会长时间悬挂在 Web 进程中，gevent 使得少量进程即可支撑数千个并发挂起的 SSE 长连接。
  * **Celery 端**：大模型调用与文档解析多为网络与外部接口 I/O，gevent 让单个 Worker 可以并发处理 100 个活跃任务而不耗尽系统内存。

### 8. Nginx（入口网关与反代优化）
* **具体做法**（见 [nginx.conf](file:///d:/GitHub/ai-review-copilot/deploy/nginx/nginx.conf)）：
  * **大文件传输**：配置 `client_max_body_size 500M` 满足大型施工图纸/Word 上传。
  * **流式长连接优化**：针对 `^/api/.+/stream/?$` 路径，显式声明：
    ```nginx
    proxy_buffering off;
    proxy_cache off;
    proxy_read_timeout 3600s;
    proxy_send_timeout 3600s;
    ```
    彻底关闭反代缓冲，保证大模型每产生一条意见前端能毫秒级收到，并保持长连接不超时。
  * **反爬与安全**：针对登录接口 `/api/auth/login/` 配置 `limit_req zone=login burst=5 nodelay`（1r/s 限流）。
  * **静态托管与直代**：前端 Vue SPA 托管在 `/` 并用 `try_files $uri $uri/ /index.html` 兜底；`/media/` 请求直接反代到 SeaweedFS。

### 9. 实时流式传输：SSE vs WebSocket vs HTTP 长轮询（高频八股）
* **为什么选用 SSE（Server-Sent Events）？**
  * **单向流特性**：模型评审意见、文档解析进度完全是“服务端向客户端推送”，无需客户端通过全双工发数据，SSE 天然契合单向流。
  * **轻量与兼容性**：基于标准 HTTP 协议（`text/event-stream`），天然穿越各类企业防火墙和代理（Nginx 支持极好）；自带断线重连（`retry`）、事件类型（`event: xxx`）与消息 ID（`id: xxx`）。
  * **对比 WebSocket**：WebSocket 是独立于 HTTP 的全双工协议，需要握手协议升级，有更复杂的心跳保活与状态管理开销，对于单向大模型推流属于“过度设计”。
