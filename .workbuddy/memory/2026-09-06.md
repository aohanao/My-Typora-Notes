- 重写 AI_Review_Copilot项目文档.md「专家经验沉淀（ReviewRule Skill）」章（严格只动本章 1868–2104 行）：改为叙述优先结构——全景闭环图 → 为什么封装成独立 Skill（3 理由）→ Settle 六步（资格/异步/缓存/结构化抽取/去重/归属）→ Consume（召回打分+正负分口径注入）→ Feedback（非对称置信度+防污染闸门）→ Clause 级缓存回放 → 工程实现速查表（代码落点对齐真实目录 settle_review_rule，节点 n0_query_cache/n1_extract_rule/n2_store_result，修正旧文 extract_review_rule 旧命名）。加速比口径用 75.7% 对齐评测矩阵。2114 行起「两个 Skill 对比」块按用户范围要求未动。
