- 重写 AI_Review_Copilot项目文档.md「Agent 评测矩阵」章节（D:/GitHub/My-Typora-Notes）：对 L1~L5 全部指标补齐口径卡——计数单位、命中判定规则（对齐 D:/GitHub/ai-review-copilot/eval/metrics/ 真实代码）、分子/分母具体含义与实测代入。要点：L1 按条款条数计数，匹配=章节号相等+字符区间 IoU≥0.60 一对一；L2 按关键事实条数（字符集覆盖≥70% 判命中），ROUGE 仅辅助；L3 引用核验按引文条数（精确子串）、覆盖度按去重 clause_id、闭环率按意见条数；L4 按陷阱场景计数；L5 漂移按三字段严格相等的样本数、采纳率分母为 AI 生成意见总数。
- 对齐 L2 摘要评测定位（依据 ai-review-copilot 代码）：评测单位 = 一条专家知识条目（Knowledge entry，写入路径，非方案/非领域大类）；触发 = 入库时 Celery llm-lite 队列 + 管理端手动 regenerate；输入 = 领域标题+基础说明+Top-20 活跃规则池（confidence/hit_count 降序）；消费方 = 新建评审任务「匹配参考依据」里的知识介绍。缺口：settle_review_rule 沉淀规则后不自动触发摘要重生成，已写入文档 L2 章节。
- 补充 L2 两级知识结构口径：Knowledge 条目（按 task_type 领域归类的容器）vs ReviewRule（一条专家意见 settle 成一条规则）；规则归属链 = task 挂载知识 → 领域下首条 → 兜底自动新建；去重键 trigger_md5+rule_kind，重复只累加 hit/reject_count。已写入文档 L2 评测定位小节。
- 重构文档第 3 章「Skill 专家经验沉淀体系」为叙述优先结构：正文只讲业务逻辑与设计决策（动机→核心概念→沉淀/消费/进化链路→防污染），代码事实（模型字段、函数、参数）全部压缩进 3.7「工程实现速查」表格当验证层。用户复习时先文字理解、代码最后自证。
- 按新面试口径重写评测体系（用户决定舍弃知识摘要层，工作流口头说四层：解析/抽取/评审/沉淀）：新映射 L1 文档解析（章节还原率96.8%/表格保真93.5%/伪标题过滤97.1%，注意此层无真实 eval runner，为面试口径设计，数字需自圆其说）→ L2 条文抽取（原 L1，F1 0.92）→ L3 评审质量（原四项体检+缓存回放漂移=0）→ L4 知识沉淀（误判抑制率+召回保持+专家采纳率72%北极星）→ L5 智能体性能（TTFT/异常率/加速比75.7%/膨胀率11.8%）。时机三分：L1/L2 离线、L3/L4 事后回放、L5 在线遥测。已同步主文档 §4、Eval-Matrix 详细章节、汇总对照表、代码映射树注释、报告 JSON、速记层 STAR/口径卡/30秒话术、CI 徽标、指标溯源小节（含 IoU 0.85→0.60 修正对齐 l1_clause_metrics.py）。未动：工作流章节仍写 5 套工作流（含 generate_knowledge_summary），与口头四层口径的差异由用户面试时自行规避。
